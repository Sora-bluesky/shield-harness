# Auto detect text files and normalize line endings to LF
# eol=lf prevents phantom diffs when tools (Claude Code, Node.js) write LF on Windows
* text=auto eol=lf

# Keep binary files as-is
*.png binary
*.jpg binary
*.ico binary
